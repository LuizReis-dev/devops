services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: k8s_python_app
    environment:
      DATABASE_URL: sqlite:////app/data/app.db
    ports:
      - "3000:3000"
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data: